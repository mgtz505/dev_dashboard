import React from 'react';
import axios from "axios";

const Weather = () => {
    return (
        <div className="widgit">
            <h2>See Weather</h2>
        </div>
    );
};

export default Weather;