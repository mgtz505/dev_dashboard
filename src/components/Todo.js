import React, { useState } from 'react';

const Todo = () => {
const [todos, setTodos] = useState([
    {text: "EXAMPLE TASK A"},
    {text: "EXAMPLE TASK B"},
    {text: "EXAMPLE TASK C"},
    {text: "EXAMPLE TASK D"},
])

    return (
        <div>
            
        </div>
    );
};

export default Todo;